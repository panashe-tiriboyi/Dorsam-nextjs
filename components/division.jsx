import React from "react";

const Division = (props) => {
  return (
    <div
      id={props.idname}
      style={{
        color: "rgba(76, 154, 207, 0.0)",
        fontSize: "15px",
        marginTop: "100px",
        position: "relative",
      }}
    >
      AboutUs
    </div>
  );
};

export default Division;
